export const metadata = {
  title: "Admin Login | Ishfabd",
  description: "Login panel for admins to access dashboard!",
};

export default function Layout({ children }) {
  return <div className="text-gray-800">{children}</div>;
}
